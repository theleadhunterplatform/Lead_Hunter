/**
 * Legacy MongoDB migration script — no longer used after Supabase migration.
 * Import LinkedIn posts via the API or seed scripts instead.
 */
console.log('This script was for MongoDB -> leadposts migration and is deprecated.');
console.log('Use npm run seed or POST /api/posts for data setup with Supabase.');
process.exit(0);
