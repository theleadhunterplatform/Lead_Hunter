{
  "name": "lead-hunter",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "redis:start": "docker compose up -d redis",
    "redis:stop": "docker compose down",
    "redis:check": "powershell -ExecutionPolicy Bypass -File scripts/redis-start.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
